
// adds the category sections for specific information obtained from google
addCategory = function($main, name) {
  return $(renderCategory({name: name, title: _.capitalize(name.replace('_', ' '))}))
   .appendTo($main);
}

// attaches the individual attractions to the document
addPlace = function($categoryEl, gid, name, src, rating) {
  var $attachEl = $categoryEl.find('ul');
  $(renderPlace({gid: gid, name: name, src: src, rating: rating}))
   .appendTo($attachEl);
}

// generates google's link to specific picture
createPhotoUrl = function(photo_reference) {
  // return 'https://maps.googleapis.com/maps/api/place/photo?maxwidth=100&photoreference=' + photo_reference + '&key=<%= ENV["GOOGLE_KEVIN_API"] %>'
  // ENV["GOOGLE_KEVIN_API"]
  return 'http://media-cdn.tripadvisor.com/media/photo-s/05/3d/a9/4f/peninsula-restaurant.jpg'
}
